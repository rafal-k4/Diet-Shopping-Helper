import { ProductdataPipe } from './productdata.pipe';

describe('ProductdataPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductdataPipe();
    expect(pipe).toBeTruthy();
  });
});
