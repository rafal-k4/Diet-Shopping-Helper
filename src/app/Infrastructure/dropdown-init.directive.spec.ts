import { DropdownInitDirective } from './dropdown-init.directive';

describe('DropdownInitDirective', () => {
  it('should create an instance', () => {
    const directive = new DropdownInitDirective();
    expect(directive).toBeTruthy();
  });
});
